# 스코프 재정렬 작업 계획 — 1일 완결 목표 정합화

작성일: 2026-07-20
상태: 계획 (실행 대기)
관련 문서: `jjugumeong_master_plan.md`, `game_design.md`, `roadmap.md`, `balancing.md`, `world_lore.md`

이 문서는 새 기능 추가 계획이 아니라, 기존 범위를 "1일 안에 완전 정복
가능한 방치형 게임"이라는 목표에 맞춰 재정렬하는 선행 작업 계획입니다.
V0.5 착수 전에 이 작업을 먼저 완료합니다.

이 문서는 원래 "6시간 완전 정복"을 목표로 작성됐던 `scope_review_6h_target.md`를
대체합니다(2026-07-20, git mv로 이름 변경 및 재작성). 6시간이라는 숫자는
실행 전 상태에서 사용자 결정으로 1일(연속 세션 기준 24시간)로 교체됐습니다.

## 1. 목적

현재 게임은 매 버전(V0.1~V0.4.5)마다 새 시스템이 추가되며 성장했지만,
총 플레이 시간이 목표와 맞는지 한 번도 측정되지 않았습니다. 동시에
기준 문서(로드맵, 세계관)는 은하 시대까지 이어지는 장기 비전을 정본으로
유지하고 있어, 매 버전 기획 시 "이게 목표 범위의 게임에 필요한가"라는
질문이 구조적으로 걸러지지 않습니다.

**목표 정의**: Alpha 25배 보상 ON 상태 1회, OFF(근사 정식 밸런스) 상태 1회,
각각 처음부터 전체 콘텐츠를 연속 세션으로 완주하며 측정합니다. 세션 중
백그라운드 자동생산은 포함하고, 여러 날에 걸친 접속 공백은 포함하지
않습니다. 목표 시간은 **24시간(1일)**이며, 이 수치 역시 아직 실제로
측정된 값이 아니라 사용자가 내린 결정값입니다 — 6시간 목표가 그랬던
것과 같은 미검증 상태이므로, 이 문서 스스로도 "재정렬했다"와 "실측을
완료했다"를 혼동하지 않습니다.

## 2. 진단 요약

| 문제 | 근거 | 영향 |
| --- | --- | --- |
| 총 플레이 시간 미측정 | `balancing.md` 결정 대기: "중간 이정표 정확한 수치 — 실제 1회 플레이 시간 측정 후 확정"이 V0.2부터 V0.4.5까지 미해결 | 목표 충족 여부를 모르는 채 메타 시스템 4개가 추가됨 |
| 로드맵이 목표보다 훨씬 큰 장기 비전을 정본으로 유지 | `roadmap.md` V0.6~V0.9, "장기 시대"(도시·국가·세계·진화·우주·은하) | 매 버전 기획 시 스코프를 걸러낼 기준이 없음 |
| 유사 미니게임 3종을 각각 따로 구현 | `dot_action_view.gd`(371줄), `hero_bond_view.gd`(199줄), 지역 사건 2택1 로직(`game_manager.gd`) | 코드 중복, 플레이어가 매번 새 조작 방식을 배워야 함 |
| V0.4 한 버전에 메타시스템 4개(보육실·역할보드·영웅·영웅유대) 동시 추가 | `roadmap.md` V0.4 항목, 저장 스키마 5~8 | 스코프 대비 상태관리·UI 복잡도 급증 |
| 세계관 설정 대비 구현 비율이 낮음 | `world_lore.md` 영웅 8명·세력 6개 vs 구현 3명·세력 0개 | 플레이에 연결되지 않은 설정이 문서 부피만 차지 |
| 절차적 애니메이션으로 "실물같은" 표현을 추구 | V0.4.5 계획, `mouse.gd` | 목표(사실적 리얼리즘)와 수단(도형 + 절차적 튜닝)이 근본적으로 불일치해 결정이 계속 미뤄짐 |
| 6시간 목표 자체도 미검증 상태로 제안됨 | 이전 버전 `scope_review_6h_target.md`가 실행 전 상태에서 1일(24h)로 대체됨 | 목표 숫자를 바꾸는 것과 실측을 완료하는 것은 별개 — 이번 재정렬도 같은 함정에 빠지지 않아야 함 |

## 3. 실행 항목

### 3.1 실측 (최우선, 다른 모든 판단의 입력값)

측정 없이는 어떤 컷/유지 결정도 근거가 없습니다.

- 방법: Alpha 배수(25배)를 켠 상태 1회, 끈 상태(정식 밸런스 근사치) 1회,
  각각 처음부터 5개 지역 + 보육실 + 역할보드 + 영웅 + 영웅유대 Lv.3까지
  완주하며 시간을 기록합니다.
- 기록 항목 (`balancing.md` 시간예산 원장·관찰 지표를 그대로 사용):
  - 첫 강화까지 걸린 시간
  - 지역별 해금까지 걸린 시간 (5개 지역 개별 기록)
  - 보육실 첫 건설 ~ 첫 성체 합류까지 걸린 시간
  - 역할보드 첫 배치까지 걸린 시간
  - 영웅 해금 ~ 유대 Lv.3 도달까지 걸린 시간
  - 총 누적 시간
- 측정 방식: 매 버전 전체 재플레이가 아니라 **증분 측정**을 원칙으로 합니다.
  이번 기준 24시간 풀 플레이(Alpha ON/OFF 각 1회)로 1~3막을 앵커링하고,
  이후 각 버전은 자신이 추가한 구간만 측정해 누적 총합에 더합니다.
- 산출물: `balancing.md`의 시간예산 원장(Table A/B)을 채우고, 결정 대기 항목
  "중간 이정표 정확한 수치"를 확정으로 전환합니다.
- 판단 기준: 총 시간이 24시간(±2~3시간) 범위를 벗어나면 3.4의 컷 후보를
  우선순위대로 적용합니다.

### 3.2 문서 스코프 경계선 명시

- `jjugumeong_master_plan.md`에 "1일 스코프 경계"(§10) 섹션을 신설합니다.
  - IN: V0.1~V0.4 구현 전체 + V0.5~V1.0("첫 정식 문명")
  - OUT (부록으로 격리): `roadmap.md` 장기 시대 6종 전부, `world_lore.md`
    문명 단계 중 "지하 국가 연합" 이후 전부, 미구현 영웅 5명·세력 6개
  - **예산이 6시간→24시간(최대 4배)으로 늘었다고 해서 장기 시대가 스코프로
    복귀하는 것은 아닙니다.** 확정 스코프 경계는 여전히 V1.0까지입니다.
- 이관 방법: 로드맵 본문에서 삭제하지 않고 "부록: 장기 시대 메모 (스코프 밖,
  참고용)"으로 이동하며, 부록 최상단에 "이 부록은 우선순위 판단에 사용하지
  않음"을 명시합니다.

### 3.3 미니게임 통합 리팩터링

- 대상: `dot_action_view.gd`, `hero_bond_view.gd`, 지역 사건 2택1 로직
- 방향: 점 개수, 순서 규칙, 실수 시 후퇴 여부, 보상 콜백만 파라미터로
  받는 공통 컴포넌트(가칭 `SequenceActionView`)로 통합
- 이 통합은 `jjugumeong_master_plan.md` §12 "연계 원칙"의 직접적인 실행
  근거입니다 — 조작 방식이 시스템마다 달라 단절되어 보이는 문제를 해소합니다.
- 선행 조건: 3.1 실측 이후 착수 (실측 대상 흐름을 리팩터링 중간에
  건드리지 않기 위함)
- 완료 조건: 기존 3개 기능(부엌 실타래·창고 철탑·편의점 통로, 영웅 유대
  임무)이 통합 컴포넌트 위에서 동일하게 동작하고 저장 스키마는 변경하지
  않습니다.

### 3.4 메타시스템 예산 정합 판단

대상: 보육실, 역할보드, 영웅, 영웅유대 4종

실측 데이터 기반 판단 기준(그랜드토탈 임계값이 아니라 **소속 막 예산 대비**로
판단):

- 소속 막(2막 무리)의 실측 시간이 목표 예산(1.5–2.5h)을 초과하면: 역할보드를
  1순위 컷/병합 후보로 검토 (탐험쥐·건설쥐 보너스를 영웅 효과로 흡수하는 방식)
- 여전히 초과하면: 보육실의 점 돌봄 미니게임을 제거하고 단순 대기시간
  기반 자동 성장으로 단순화
- 총 시간이 24시간에 크게 못 미치면: 컷은 불필요하며, 대신 5개 지역
  각각의 밀도를 높이는 방향으로 재배분
- 허용오차는 24시간 규모에 맞춰 ±1시간(6시간 기준)에서 ±2~3시간으로 조정합니다.

### 3.5 아트 방향 결정

- 결정 대기 보드에 신규 항목 "리얼리즘 vs 스타일화"를 추가합니다.
- 옵션 A: 걷기·운반·정지 3~5프레임 스프라이트를 최소로 확보해 절차적
  모션과 결합
- 옵션 B: 리얼리즘을 포기하고 도형 기반 스타일을 정식 아트 방향으로
  확정, `game_design.md` 4절의 "실물처럼" 표현을 스타일 목표로 수정
- 1일 규모 게임과 개발 리소스를 고려하면 옵션 B를 기본값으로 권장하되
  최종 결정은 사용자가 내립니다.
- 이 항목은 시간예산 재정렬과 무관하게 그대로 유지됩니다.

### 3.6 향후 버전 계획 체크리스트 도입

`jjugumeong_master_plan.md` §11 "시간예산 관리 원칙"에 정의된 6항목 체크리스트를
**V0.5부터** 모든 `docs/plans/vX.Y.Z_*.md` 문서에 적용합니다. V0.1–V0.4.5
기존 계획 문서는 소급 적용하지 않습니다.

### 3.7 게임 경험 정합성 원칙 도입

`jjugumeong_master_plan.md` §12에 5개 원칙(연계·개체 독립성·의도된 재미
정렬·능동 참여 비중·이동 목표지점 정확도)을 등록하고, 추적이 필요한 항목을
결정 대기 보드(§13)에 반영합니다. 이동 정확도는 실제 버그 여부 확인 후
별도 처리(§12.2 분기 참고).

## 4. 실행 순서와 의존관계

1. 3.1 실측 — 다른 모든 판단의 입력값
2. 3.5 아트 방향 결정 — 실측과 독립적으로 병행 가능
3. 3.2 문서 스코프 경계선 — 3.1 결과 반영
4. 3.4 메타시스템 예산 정합 판단 — 3.1 결과로 확정
5. 3.3 미니게임 통합 리팩터링 — 3.4 확정 이후, 남길 시스템 범위가
   정해진 뒤 착수
6. 3.6 버전 계획 체크리스트 도입, 3.7 게임 경험 정합성 원칙 도입 — 문서
   재정렬 작업과 함께 즉시 반영 가능, 3.1 실측과 독립

## 5. 완료 조건

- `balancing.md`에 시간예산 원장(Table A/B)이 신설되고 결정 대기 항목
  "중간 이정표 정확한 수치"의 결정 기준이 원장으로 재연결됨
- `jjugumeong_master_plan.md`에 1일 스코프 IN/OUT 경계와 막별 예산표가 명시됨
- `roadmap.md` 장기 항목이 부록으로 분리되어 우선순위 판단에서 제외됨
- 아트 방향이 결정되고 `game_design.md`에 반영됨
- 향후 버전 계획 체크리스트가 `jjugumeong_master_plan.md`에 추가되고
  V0.5부터 적용됨이 명시됨
- 게임 경험 정합성 원칙 5개가 `jjugumeong_master_plan.md`에 등록되고
  추적 필요 항목이 결정 대기 보드에 반영됨
- 실측 결과에 따라 메타시스템 4종 중 컷·축소 대상이 확정됨 (후속, 이 작업
  범위 밖)
- (후속) 통합 미니게임 컴포넌트가 기존 저장 스키마를 깨지 않고 동작함

## 6. 제외 범위

- 이 작업은 신규 콘텐츠를 추가하지 않습니다.
- 로드맵 항목의 완전 삭제는 하지 않습니다 (부록 이관만 수행합니다).
- 장기 시대(6막) 콘텐츠는 예산 증가와 무관하게 스코프 밖으로 유지합니다.
- V0.5 지하마을 착수는 이 작업 완료 후로 미룹니다.
- 실제 24시간 실측 플레이와 코드 변경은 이 문서 작업의 범위 밖이며, 다음
  단계(3.1)로 미룹니다.
