# 2026-07-19 — V0.2.6 시각 성장 정합성

## 배경과 계획

V0.2 기능은 대부분 구현됐지만 외형 성장의 단계가 화면 코드에 흩어져 있었고,
다음 보상 진행도도 UI가 직접 계산하고 있었습니다. 구현 전에
[`V0.2.6 작업 계획`](../plans/v0.2.6_visual_progression.md)에 범위, 제외 범위,
책임과 완료 조건을 먼저 확정했습니다.

지역 선택, 위험 사건과 직업은 V0.3 범위이므로 이번 작업에 포함하지 않았습니다.
영구 저장 데이터도 추가하지 않아 저장 스키마 1을 유지했습니다.

## 구현

### 외형 티어 단일 기준

- `VisualProgression`을 추가해 속도 Lv.0/1/3/6, 운반 Lv.0/1/3/6,
  쥐구멍 Lv.1/2/3/4/5 경계를 단일 규칙으로 정의
- 속도 강화에 목도리, 운동화와 숙련 질주 표시 추가
- 운반 강화에 천 주머니, 배낭과 전문 운송 표시 추가
- UI에서 현재 속도·운반 장비 이름 표시
- 쥐구멍 보강, 저장 선반과 조명도 같은 티어 규칙 사용

### 보이는 성장 연결 수정

배경이 있는 5개 지역에서 쥐구멍 성장 장식과 생활쥐 그리기 함수가 정의돼
있었지만 실제 그리기 흐름에서 호출되지 않는 상태를 확인했습니다.

- 지역 배경 위에 쥐구멍 성장 장식을 다시 연결
- 보유 쥐 수에 따른 굴 앞 생활쥐 표현을 다시 연결
- 외부 에셋이 없어도 티어 장식은 기본 도형으로 표현

### 다음 보상 책임 분리

- 다음 지역 또는 전체 지역 해금 뒤 군락 목표를
  `GameManager.get_next_reward_summary()`에서 계산
- 제목, 현재값, 목표값, 남은 값과 0~100 진행도를 상태 API로 제공
- `GameUI`에서는 지역 임계값과 군락 구간 계산을 제거하고 결과만 표시

### 회귀 테스트

- 세 성장축의 모든 티어 경계 테스트
- 다음 지역 50% 진행과 군락 목표 50% 진행 테스트
- 기존 768×1024 세로형에 1366×1024 가로형 UI 레이아웃 검증 추가

## 검증

Godot 4.7 stable 기준:

- headless import와 GDScript 파싱 통과
- `tests/test_runner.tscn`: `JJUGUMEONG V0.2 tests: PASS`
- 메인 씬 3프레임 headless 실행 오류 없음
- `git diff --check` 통과

## 남은 완료 게이트

자동 검증으로 iPad Safari의 실제 글자 크기, 터치 면적과 발견 카드의 체감 시간을
판정할 수는 없습니다. 다음 작업은 대표 iPad 가로·세로 실기기에서 다음 항목을
확인하는 것입니다.

1. 상·하단 정보와 성장 버튼 잘림 여부
2. 강화 장비와 쥐구멍 장식의 실제 식별성
3. 지역 발견 카드 3초 표시가 자동 생산 흐름을 방해하지 않는지
4. Safari 탭 복귀와 PWA 재실행 뒤 저장 유지

## V0.2.7 — iPad 실기기 검증 준비

구현 전에
[`V0.2.7 작업 계획`](../plans/v0.2.7_ipad_validation.md)에 자동 검증 범위와
실제 iPad에서만 판정할 항목을 분리했습니다.

### 빌드 식별

- `data/build.json`을 제품명, 버전과 개발 단계의 단일 기준으로 추가
- 게임 상단 제목을 고정 문자열 대신 빌드 데이터에서 구성
- Web HTML에 `r4 0.2.7` 빌드와 `V0.2 Alpha` 단계 메타데이터 삽입
- 서비스 워커 캐시 버전에 표시 빌드를 포함해 이전 배포 캐시와 구분

### Web 릴리스 자동 검사

- `tools/verify-web-build.mjs` 추가
- 필수 Web 파일과 비어 있지 않은 산출물 검사
- `viewport-fit=cover`, 동적 viewport, 고정 canvas와 터치 차단 검사
- PWA standalone, landscape와 144/180/512 아이콘 검사
- 서비스 워커 즉시 활성화와 gzip WASM 캐시 검사
- 원본 WASM 제거, gzip 헤더와 브라우저 압축 해제 로더 검사

### 검증 결과

- Godot 4.7 headless import와 전체 V0.2 테스트 통과
- Godot 4.7 Web release export 성공
- `npm run prepare:game` 성공: WASM 37.7 MiB → gzip 9.7 MiB
- `npm run verify:game` 성공
- Vinext 프로덕션 빌드 성공
- 저장 필드 변경 없음, 스키마 1 유지

### 다음 확인

실제 iPad 결과는
[`V0.2 iPad 체크리스트`](../testing/ipad_v0.2_checklist.md)에 기록합니다.
빈 항목은 통과로 간주하지 않으며, 실기기 결과 전까지 V0.2 상태를
`실기기 검증 대기`로 유지합니다.
